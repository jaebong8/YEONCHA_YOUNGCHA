import Head from "next/head";

const Home: React.FC = () => {
    return (
        <>
            <Head>
                <title>연차영차</title>
                <meta name="viewport" content="width=device-width, initial-scale=1" />
            </Head>
        </>
    );
};

export default Home;
